const a=[;
var n:integer;
i,j:integer;
c:char;
s:string;
pos:integer;
begin
    readln(n);
    for i:=1 to n do
        begin
        readln(s);
        for j:=1 to length(s) do begin
            pos:=1;
            if s[j]=' ' then begin
                if 
        end;
    
end.