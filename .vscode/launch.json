{
    "version": "0.2.0",
    "configurations": [
        {

            "preLaunchTask": "g++",

        },
        {
            "type": "lldb-mi",
            "request": "launch",
            "name": "Launch Program",
            "target": "${workspaceRoot}/temp",
            "cwd": "${workspaceRoot}"
            
        }

        
    ]
}